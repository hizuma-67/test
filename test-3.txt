
--[[
    Hizuma_67 - PREMIER EDITION (FIXED, OPTIMIZED & UPGRADED)
    Cập nhật: 
    - Giữ nguyên 100% code gốc của người dùng.
    - FIX LỖI LOADING: Loading chạy xong mới hiện Key System.
    - Thêm Tab Âm Nhạc PROMAX (Hệ thống quản lý Sound ID thông minh).
    - Slider chỉnh âm lượng (Volume) phía trên nút Stop.
]]

----------------------------------------------------------------
-- HỆ THỐNG GLOBAL BAN (KIỂM TRA TRƯỚC KHI CHẠY)
----------------------------------------------------------------
local BannedUsers = {
    [123456789] = "Lý do: Sử dụng bản crack", -- Ví dụ ID bị ban
}

if BannedUsers[game:GetService("Players").LocalPlayer.UserId] then
    game:GetService("Players").LocalPlayer:Kick("❌ Bạn đã bị Global Ban khỏi 6_7 HUB. Lý do: " .. BannedUsers[game:GetService("Players").LocalPlayer.UserId])
    return
end

----------------------------------------------------------------
-- HỆ THỐNG DISCORD WEBHOOK (LOGGER) - PHIÊN BẢN FIX OBFUSCATE
----------------------------------------------------------------
local Webhook_URL = "https://discord.com/api/webhooks/1474136893279961211/PNvOmJGxcQT6CYZWqUknu0Pnsjs3dqNWrMUVdDW5rROj-N1ujRXHAg4YUO80JyxLBt6T" 
local SafeRequest = (getgenv and (getgenv().request or getgenv().http_request)) or (syn and syn.request) or (http and http.request) or request or http_request
local function SendWebhook()
    task.spawn(function()
        pcall(function()
            local HttpService = game:GetService("HttpService")
            local MarketplaceService = game:GetService("MarketplaceService")
            local Players = game:GetService("Players")
            local LocalPlayer = Players.LocalPlayer
            local PlaceID = game.PlaceId
            local JobId = game.JobId
            
            local AccountAge = LocalPlayer.AccountAge or 0
            
            local Executor = "Unknown"
            if identifyexecutor then Executor = identifyexecutor() 
            elseif (syn and syn.request) then Executor = "Synapse X"
            elseif (fluxus) then Executor = "Fluxus"
            elseif (Arceus) then Executor = "Arceus X"
            end
            
            local Country = "Unknown"
            local req_func = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
            
            if req_func then
                local success, response = pcall(function()
                    return req_func({Url = "http://ip-api.com/json/", Method = "GET"})
                end)
                if success and response and response.Body then
                    local decoded = HttpService:JSONDecode(response.Body)
                    Country = decoded.country or "Unknown"
                end
            end

            local GameName = "Unknown"
            pcall(function()
                GameName = MarketplaceService:GetProductInfo(PlaceID).Name
            end)

            local JoinCode = "game:GetService('TeleportService'):TeleportToPlaceInstance("..PlaceID..", '"..JobId.."', game.Players.LocalPlayer)"

            local data = {
                ["embeds"] = {{
                    ["title"] = "🚀 Script Executed (Obfuscated Version)",
                    ["description"] = "Một người dùng đã thực thi scprit",
                    ["color"] = 0x00FFFF, 
                    ["fields"] = {
                        {["name"] = "👤 User Status", ["value"] = "Một người chơi đã sử dụng script", ["inline"] = false},
                        {["name"] = "⏳ Account Age", ["value"] = tostring(AccountAge) .. " days", ["inline"] = true},
                        {["name"] = "🌍 Location", ["value"] = Country, ["inline"] = true},
                        {["name"] = "💻 Executor", ["value"] = Executor, ["inline"] = true},
                        {["name"] = "🎮 Game", ["value"] = GameName, ["inline"] = false},
                        {["name"] = "🔗 Join Server Script", ["value"] = "```lua\n" .. JoinCode .. "\n```", ["inline"] = false}
                    },
                    ["footer"] = {["text"] = "Hizuma_67 Logger System"},
                    ["timestamp"] = os.date("!%Y-%m-%dT%H:%M:%SZ")
                }}
            }
            
            if req_func then
                req_func({
                    Url = Webhook_URL, 
                    Method = "POST", 
                    Headers = {["Content-Type"] = "application/json"}, 
                    Body = HttpService:JSONEncode(data)
                })
            end
        end)
    end)
end

SendWebhook()

if not game:IsLoaded() then game.Loaded:Wait() end

local Players = game:GetService("Players")
local Player = Players.LocalPlayer
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local MarketplaceService = game:GetService("MarketplaceService")
local VirtualUser = game:GetService("VirtualUser")
local Lighting = game:GetService("Lighting")
local UserInputService = game:GetService("UserInputService")
local ContextActionService = game:GetService("ContextActionService")

----------------------------------------------------------------
-- HỆ THỐNG ÂM THANH & NHẠC (UPDATE PROMAX)
----------------------------------------------------------------
local CurrentMusic = nil
local CurrentVolume = 2 

local function PlaySound(id)
    task.spawn(function()
        pcall(function()
            local s = Instance.new("Sound")
            s.SoundId = "rbxassetid://" .. id
            s.Volume = 1
            s.Parent = workspace 
            s:Play()
            s.Ended:Connect(function() s:Destroy() end)
        end)
    end)
end

local function PlayMusic(id, name)
    pcall(function()
        if CurrentMusic then
            CurrentMusic:Stop()
            CurrentMusic:Destroy()
            CurrentMusic = nil
        end
        
        local s = Instance.new("Sound")
        s.Name = "67_Music_System"
        s.SoundId = "rbxassetid://" .. id
        s.Volume = CurrentVolume
        s.Looped = true
        s.Parent = game:GetService("SoundService") 
        s:Play()
        CurrentMusic = s
        
        if _G.Rayfield then
            _G.Rayfield:Notify({
                Title = "🎶 Music Playing",
                Content = "Đang phát: " .. (name or id),
                Duration = 5,
                Image = 4483362458
            })
        end
    end)
end

----------------------------------------------------------------
-- HỆ THỐNG GUI AN TOÀN
----------------------------------------------------------------
local function GetSafeGui()
    local target = nil
    local success = pcall(function()
        target = game:GetService("CoreGui")
    end)
    if success and target then return target end
    return Player:WaitForChild("PlayerGui")
end

local ScreenParent = GetSafeGui()

local AFKGui = Instance.new("ScreenGui")
AFKGui.Name = "67_AFK_System_" .. math.random(100, 999)
AFKGui.IgnoreGuiInset = true
AFKGui.DisplayOrder = 5 
AFKGui.Parent = ScreenParent

local AFKFrame = Instance.new("Frame")
AFKFrame.Size = UDim2.new(1, 0, 1, 0)
AFKFrame.Visible = false
AFKFrame.BorderSizePixel = 0
AFKFrame.ZIndex = 1
AFKFrame.Parent = AFKGui

----------------------------------------------------------------
-- 1. HỆ THỐNG LOADING GALAXY STYLE (FIXED)
----------------------------------------------------------------
local LoadingGui = Instance.new("ScreenGui")
LoadingGui.Name = "6_7_Extended_Load"
LoadingGui.IgnoreGuiInset = true
LoadingGui.DisplayOrder = 10000
LoadingGui.Parent = ScreenParent

local Overlay = Instance.new("Frame")
Overlay.Size = UDim2.new(1, 0, 1, 0)
Overlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
Overlay.BackgroundTransparency = 1
Overlay.Parent = LoadingGui
TweenService:Create(Overlay, TweenInfo.new(0.5), {BackgroundTransparency = 0.5}):Play()

local GlowFrame = Instance.new("Frame")
GlowFrame.Size = UDim2.new(0, 380, 0, 200)
GlowFrame.Position = UDim2.new(0.5, -190, 0.5, -100)
GlowFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GlowFrame.BackgroundTransparency = 0.8
GlowFrame.Parent = LoadingGui
local GlowCorner = Instance.new("UICorner")
GlowCorner.CornerRadius = UDim.new(0, 15)
GlowCorner.Parent = GlowFrame

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 360, 0, 180)
MainFrame.Position = UDim2.new(0.5, -180, 0.5, -90)
MainFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10) 
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = LoadingGui

local UniverseBg = Instance.new("ImageLabel")
UniverseBg.Name = "UniverseBackground"
UniverseBg.Size = UDim2.new(1, 0, 1, 0)
UniverseBg.BackgroundTransparency = 1
UniverseBg.Image = "rbxassetid://5323900286" 
UniverseBg.ScaleType = Enum.ScaleType.Crop
UniverseBg.ImageTransparency = 0.1 
UniverseBg.ZIndex = 1
UniverseBg.Parent = MainFrame

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 12)
MainCorner.Parent = MainFrame

local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 3.5
UIStroke.Color = Color3.fromRGB(255, 0, 0)
UIStroke.Parent = MainFrame

local TitleLoad = Instance.new("TextLabel")
TitleLoad.Size = UDim2.new(1, 0, 0, 60)
TitleLoad.Position = UDim2.new(0, 0, 0.1, 0)
TitleLoad.Text = "6_7 HUB"
TitleLoad.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLoad.TextSize = 36
TitleLoad.Font = Enum.Font.GothamBold
TitleLoad.BackgroundTransparency = 1
TitleLoad.ZIndex = 2
TitleLoad.Parent = MainFrame

local IconLoadRight = Instance.new("ImageLabel")
IconLoadRight.Name = "IconLoadRight"
IconLoadRight.Size = UDim2.new(0, 35, 0, 35)
IconLoadRight.Position = UDim2.new(0.5, 75, 0.15, 0)
IconLoadRight.BackgroundTransparency = 1
IconLoadRight.Image = "rbxassetid://11478378840"
IconLoadRight.ZIndex = 3
IconLoadRight.Parent = MainFrame

local TitleGlow = Instance.new("UIStroke")
TitleGlow.Thickness = 2
TitleGlow.Color = Color3.fromRGB(255, 0, 0)
TitleGlow.Transparency = 0.5
TitleGlow.Parent = TitleLoad

local TitleGradient = Instance.new("UIGradient")
TitleGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 255, 255)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 255, 255))
})
TitleGradient.Parent = TitleLoad

local BarBack = Instance.new("Frame")
BarBack.Size = UDim2.new(0, 280, 0, 7)
BarBack.Position = UDim2.new(0.5, -140, 0.68, 0)
BarBack.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
BarBack.Parent = MainFrame
BarBack.ZIndex = 2

local BarFill = Instance.new("Frame")
BarFill.Size = UDim2.new(0, 0, 1, 0)
BarFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
BarFill.Parent = BarBack
BarFill.ZIndex = 2
local FillGradient = Instance.new("UIGradient")
FillGradient.Color = ColorSequence.new(Color3.fromRGB(0, 255, 255), Color3.fromRGB(0, 150, 255))
FillGradient.Parent = BarFill

local StatusText = Instance.new("TextLabel")
StatusText.Size = UDim2.new(1, 0, 0, 20)
StatusText.Position = UDim2.new(0, 0, 0.82, 0)
StatusText.Text = "Khởi động hệ thống..."
StatusText.TextColor3 = Color3.fromRGB(255, 255, 255) 
StatusText.TextSize = 12
StatusText.Font = Enum.Font.GothamMedium
StatusText.BackgroundTransparency = 1
StatusText.ZIndex = 2
StatusText.Parent = MainFrame

task.spawn(function()
    while LoadingGui and LoadingGui.Parent do
        local t = tick()
        local rainbow = Color3.fromHSV((t % 5) / 5, 0.8, 1)
        if UIStroke and UIStroke.Parent then UIStroke.Color = rainbow end
        if GlowFrame and GlowFrame.Parent then GlowFrame.BackgroundColor3 = rainbow end
        if TitleGlow and TitleGlow.Parent then TitleGlow.Color = rainbow end
        if TitleGradient and TitleGradient.Parent then TitleGradient.Rotation = math.sin(t) * 45 end
        task.wait(0.05)
    end
end)

----------------------------------------------------------------
-- LOGIC HỆ THỐNG RAYFIELD (ENCAPSULATED TO FIX TIMING)
----------------------------------------------------------------
local function StartRayfield()
    local PlaceID = game.PlaceId
    local UniverseID = game.GameId
    local GameName = "Unknown"
    pcall(function()
        local gameInfo = MarketplaceService:GetProductInfo(PlaceID)
        GameName = gameInfo.Name
    end)
    local LowGameName = string.lower(GameName)

    local isJJS = false
    if string.find(LowGameName, "jujutsu") or string.find(LowGameName, "shenanigan") or string.find(LowGameName, "jjs") or 
       UniverseID == 3760416955 or PlaceID == 10450270085 or PlaceID == 15510618037 or PlaceID == 11935914792 then
        isJJS = true
    end

    local isTSB = false
    if string.find(LowGameName, "strongest") or UniverseID == 3751717616 or PlaceID == 10449761463 then
        isTSB = true
    end

    local isBloxFruit = false
    if string.find(LowGameName, "blox fruit") or PlaceID == 2753915549 or PlaceID == 4442272183 or PlaceID == 7449423635 then
        isBloxFruit = true
    end
   
   local isJumpShowdown = false
    if string.find(LowGameName, "jump showdown") or string.find(LowGameName, "jump showndown") or PlaceID == 18519254033 then
        isJumpShowdown = true
    end
    
    _G.AntiKick = true
    task.spawn(function()
        pcall(function()
            local mt = getrawmetatable(game)
            local oldNamecall = mt.__namecall
            setreadonly(mt, false)
            mt.__namecall = newcclosure(function(self, ...)
                local method = getnamecallmethod()
                if _G.AntiKick and self == Player and (method == "Kick" or method == "kick") then
                    return nil
                end
                return oldNamecall(self, ...)
            end)
            setreadonly(mt, true)
        end)
    end)

    local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()
    _G.Rayfield = Rayfield 

    local Window = Rayfield:CreateWindow({
       Name = "6_7 HUB (v2.8) | " .. (GameName or "Main Menu"),
       LoadingTitle = "Đang mở Menu 6_7...",
       LoadingSubtitle = "Đang tải dữ liệu...",
       ConfigurationSaving = { Enabled = true, FolderName = "Hizuma_Config", FileName = "Main" },
       KeySystem = false
    })

    PlaySound("117084877592917")

    local function Notify(title, content)
        Rayfield:Notify({ Title = title, Content = content, Duration = 4, Image = 4483362458 })
    end

    -- [TAB JJS]
    if isJJS then
        local TabJJS = Window:CreateTab("JJS Menu", 88421894421545)
        TabJJS:CreateLabel([[Phiên bản v2.8]])
        TabJJS:CreateSection("📌 Jujutsu Shenanigans")
        TabJJS:CreateButton({ Name = "🔥 TBO Script (JJS)", Callback = function() 
            Notify("TBO HUB", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/cool5013/TBO/main/TBOscript"))() 
            Notify("TBO HUB", "Hoàn thành load script!")
        end })
        TabJJS:CreateButton({ Name = "Script Uppercut And Downslam (JJS)", Callback = function() 
            Notify("Uppercut And Downslam", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/ggab2351-stack/Jjs/refs/heads/main/Uppercutanddownslam"))() 
            Notify("Uppercut And Downslam", "Hoàn thành load script!")
        end })
        TabJJS:CreateButton({ Name = "Script Black Flash (JJS)", Callback = function() 
            Notify("Black Flash", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/ggab2351-stack/Jjs/refs/heads/main/obfuscated_script-1770002780541.lua.txt"))()
            Notify("Black Flash", "Hoàn thành load script!")
        end })
        TabJJS:CreateButton({ Name = "Script Naoya (JJS)", Callback = function() 
            Notify("Naoya", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/ggab2351-stack/Jjs/refs/heads/main/Uppercutanddownslam"))() 
            Notify("Naoya", "Hoàn thành load script!")
        end })
    end

    -- [TAB TSB]
    if isTSB then
        local TabTSB = Window:CreateTab("TSB Menu", 92607990285302)
        TabTSB:CreateLabel([[Phiên bản v2.8]])
        TabTSB:CreateSection("📌 The Strongest Battlegrounds")
        TabTSB:CreateButton({ Name = "🔥 Script CarbonHub", Callback = function()
            Notify("CarbonHub", "Đang load script...") 
            loadstring(game:HttpGet("https://raw.githubusercontent.com/CStudios-Dev/csLoader.lua/main/CSLoader.lua"))()
            Notify("CarbonHub", "Hoàn thành load script!")
        end })
        TabTSB:CreateButton({ Name = "VexonHub", Callback = function()
            Notify("VexonHub", "Đang load script...") 
            loadstring(game:HttpGet("https://raw.githubusercontent.com/DiosDi/VexonHub/refs/heads/main/VexonHub"))()
            Notify("VexonHub", "Hoàn thành load script!")
        end })
        TabTSB:CreateButton({ Name = "Merebennie Hub V1.8 (Hỗ trợ Delta,Codex,Cryptic)", Callback = function() 
            Notify("Merebennie Hub", "Đang load script...") 
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/5f20686e7f7949068518fdad97821c57098f285781a1c21cc2be90d5ef64f278.lua"))()
            Notify("Merebennie Hub", "Hoàn thành load script!")
        end })
        
        local TabTech = Window:CreateTab("Tech", 17313313998)
        TabTech:CreateSection("📌 Tùy chỉnh Tech (TSB)")
        TabTech:CreateButton({ Name = "🔥 Supa Tech V3", Callback = function() 
            Notify("Supa Tech V3", "Đang load script...")
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/ea0b7cbd8c395e01ec38271794b2559808d26501bd6e6e30c48660759a7db7b3.lua"))()
            Notify("Supa Tech V3", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = " Backdash Cancel", Callback = function()
            Notify("Backdash Cancel", "Đang load script...")
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/0b57119c46c0267e6791f789ace2ffac7b752a63224d86a0b6f95d68aec099ac.lua"))()
            Notify("Backdash Cancel", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = " BOOMY LoopDash V2", Callback = function() 
            Notify("BOOMY LoopDash V2", "Đang load script...")
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/84e2bd29cccc0f5302267e4dc952cff6816db4af36416cbd477daaa26d60863d.lua"))()
            Notify("BOOMY LoopDash V2", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = " Instant Twisted Old", Callback = function() 
            Notify("Instant Twisted Old", "Đang load script...")
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/1e18721250d10562953e57cd75a2e7e4151b7d20e876930c0f394056d253b3fd.lua"))()
            Notify("Instant Twisted Old", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = " Side Dash assist V3", Callback = function() 
            Notify("Side Dash assist V3", "Đang load script...")
            loadstring(game:HttpGet("https://api.getpolsec.com/scripts/hosted/23bcf4264b586dc93b16a9b054eddae259938b7421ac5096353079b2e9d74e24.lua"))()
            Notify("Side Dash assist V3", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = "Best Oreo Script ", Callback = function() 
            Notify("Best Oreo Script", "Đang load script...")
            loadstring(game:HttpGet("https://pandadevelopment.net/virtual/file/e6419ee43f9e7f1f"))()
            Notify("Best Oreo Script", "Hoàn thành load script!")
        end })
        TabTech:CreateButton({ Name = "M1 Reset", Callback = function() 
            Notify("M1 Reset", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Slaphello/M1-Reset-And-Emote-Dash-TSB-OLD-/refs/heads/main/M1R%26ED%20TSB"))()
            Notify("M1 Reset", "Hoàn thành load script!")
        end })
    end

    -- [TAB BLOX FRUIT]
    if isBloxFruit then
        local TabBF = Window:CreateTab("Blox Fruit", 15370410333)
        TabBF:CreateLabel([[Phiên bản v2.8]])
        TabBF:CreateSection("📌 Blox Fruits")
        TabBF:CreateButton({ Name = "🔥 Gravity Hub", Callback = function() 
            Notify("Gravity Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Dev-GravityHub/BloxFruit/refs/heads/main/Main.lua"))()
            Notify("Gravity Hub", "Hoàn thành load script!")
            end })
            TabBF:CreateButton({ Name = "Xeno Hub", Callback = function() 
            Notify("Xeno Hub", "Đang load script...")
            loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/245375592509cc0bcb3526982431ab18.lua"))()
            Notify("Xeno Hub", "Hoàn thành load script!")
            end })
            TabBF:CreateButton({ Name = "NightMystic", Callback = function() 
            Notify("NightMystic", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Dev-NightMystic/Night-Mystic-/refs/heads/main/NightMystic"))()
            Notify("NightMystic", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "BlueX Hub", Callback = function() 
            Notify("BlueX Hub", "Đang load script...")
            _G.AutoTranslate = true
_G.SaveConfig = true
loadstring(game:HttpGet("https://raw.githubusercontent.com/Dev-BlueX/BlueX-Hub/refs/heads/main/Main.lua"))()
            Notify("BlueX Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "QuantumOnyx", Callback = function() 
            Notify("QuantumOnyx", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/flazhy/QuantumOnyx/refs/heads/main/QuantumOnyx.lua"))()
            Notify("QuantumOnyx", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Rise Hub", Callback = function() 
            Notify("Rise Hub", "Đang load script...")
            getgenv().Team = "Pirates" --Marines / Pirates
loadstring(game:HttpGet('https://raw.githubusercontent.com/asher-realrise/project/refs/heads/main/loadder.lua'))()
            Notify("Rise Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Meox hub", Callback = function() 
            Notify("Meox Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/VanHoangIOS/MeoXHub/refs/heads/main/Main.lua"))()
            Notify("Meox Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Bacon Hub", Callback = function() 
            Notify("Bacon Hub", "Đang load script...")
            repeat wait() until game:IsLoaded() and game.Players.LocalPlayer 
getgenv().team = "Pirates" -- Pirates or Marines
loadstring(game:HttpGet("https://raw.githubusercontent.com/vinh129150/hack/refs/heads/main/BaconHub.lua"))()
            Notify("Bacon Hub", "Hoàn thành load script!")          
        end })
        TabBF:CreateButton({ Name = "Orange Hub", Callback = function() 
            Notify("Orange Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/HieuDepTrai-Z/Dev_Orange/refs/heads/main/OrangeHub.lua"))()
            Notify("Orange Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Dragon Hub", Callback = function() 
            Notify("Dragon Hub", "Đang load script...")
            repeat wait() until game:IsLoaded() and game.Players.LocalPlayer 
getgenv().team = "Pirates" -- Pirates or Marines
loadstring(game:HttpGet("https://raw.githubusercontent.com/dragonhubdev/dragonwitheveryone/refs/heads/main/Main-BF.lua"))()
            Notify("Dragon Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Zynex hub", Callback = function() 
            Notify("Zynex hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Hirokai-Script-make/Zynexhubbloxfruit/refs/heads/main/ZynexHub-BloxFruit-redz.lua"))()
            Notify("Zynex hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Abacaxi Hub", Callback = function() 
            Notify("Abacaxi Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/real33ms/BloxFruits/refs/heads/main/AbacaxiHubOfc.lua"))()
            Notify("Abacaxi Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Hdanh Hub", Callback = function() 
            Notify("Hdanh Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/hdanhvip/hdanhhub/refs/heads/main/HdanhHubBeta.lua.txt"))()
            Notify("Hdanh Hub", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Longhihi", Callback = function() 
            Notify("Longhihi", "Đang load script...")
            loadstring(game:HttpGet(" https://raw.githubusercontent.com/longhihilonghihi-hub/Devs-LongHiHiV2/refs/heads/main/MainV2.txt"))()
            Notify("Longhihi", "Hoàn thành load script!")
        end })
        TabBF:CreateButton({ Name = "Syntrax Hub", Callback = function() 
            Notify("Syntrax Hub", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/DevHub-roblox/script-SYNTRAX-Hub/refs/heads/main/Game/Bloxfruit/SYNTRAXHub/main.txt.lua"))()
            Notify("Syntrax Hub", "Hoàn thành load script!")
        end })
    end
        if isJumpShowdown then
        local TabJS = Window:CreateTab("Jump Showdown", 130691080673007) 
        TabJS:CreateLabel([[Phiên bản v2.8 (Chế độ Copy)]])
        TabJS:CreateSection("📌 Script Jump Showdown")
        TabJS:CreateButton({ 
            Name = "🔥 Copy Script Jump Showdown", 
            Callback = function() 
                -- Bọc pcall để an toàn khi dùng Obfuscator
                local success, err = pcall(function()
                    -- ĐỔI LINK DƯỚI ĐÂY THÀNH LINK SCRIPT CỦA BẠN
                    setclipboard([[script_key="ur key here"
loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/01868f68b39e5d2e8e789d97b050ee07.lua"))()
KEY : https://ads.luarmor.net/get_key?for=Kitsu_Key_Access-EDECddRlqemy]])
                end)
    setclipboard([[script_key="ur key here"
loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/01868f68b39e5d2e8e789d97b050ee07.lua"))()
KEY : https://ads.luarmor.net/get_key?for=Kitsu_Key_Access-EDECddRlqemy]])
                if success then
                    Notify("Thành công", "Đã sao chép Script 1!\nHãy dán (Ctrl+V) vào Executor.")
                else
                    Notify("Lỗi", "Executor của bạn không hỗ trợ tính năng sao chép (setclipboard).")
                end
            end 
        })
        end
    -- [TAB TIỆN ÍCH]
    if not isJJS and not isTSB and not isBloxFruit and not isJumpShowdown then
        local TabUtility = Window:CreateTab("Script", 11570895459)
        TabUtility:CreateLabel([[Game này hiện tại chưa hỗ trợ!(v2.8)]])
        TabUtility:CreateSection("📌 Script Tiện ích")
        TabUtility:CreateButton({ Name = "🔥 Infinite Yield (FE Admin)", Callback = function() 
            Notify("Infinite Yield", "Đang load script...")
            loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))() 
            Notify("Infinite Yield", "Hoàn thành load script!")
        end })
        TabUtility:CreateButton({ Name = "FE Animations", Callback = function() 
            Notify("FE Animations", "Đang load script...")
            loadstring(game:HttpGet("https://raw.githubusercontent.com/BeemTZy/Motiona/refs/heads/main/source.lua"))()
            Notify("FE Animations", "Hoàn thành load script!")
        end })
    end

    -- [TAB NGƯỜI CHƠI]
    local TabPlayer = Window:CreateTab("Người Chơi", 8797391485)
    TabPlayer:CreateSection("📌 Tùy chỉnh nhân vật")
    TabPlayer:CreateSlider({
       Name = "WalkSpeed",
       Range = {16, 500},
       Increment = 1,
       CurrentValue = 16,
       Callback = function(Value)
           if Player.Character and Player.Character:FindFirstChild("Humanoid") then Player.Character.Humanoid.WalkSpeed = Value end
       end,
    })

    local originalBrightness = Lighting.Brightness
    local originalClockTime = Lighting.ClockTime
    local originalFogEnd = Lighting.FogEnd
    local originalGlobalShadows = Lighting.GlobalShadows

    TabPlayer:CreateToggle({
        Name = "Bật Full Bright",
        CurrentValue = false,
        Callback = function(Value)
            if Value then
                Lighting.Brightness = 2
                Lighting.ClockTime = 14
                Lighting.FogEnd = 100000
                Lighting.GlobalShadows = false
                Notify("Full Bright", "Đã bật chế độ sáng!")
            else
                Lighting.Brightness = originalBrightness
                Lighting.ClockTime = originalClockTime
                Lighting.FogEnd = originalFogEnd
                Lighting.GlobalShadows = originalGlobalShadows
                Notify("Full Bright", "Đã tắt, quay về trạng thái gốc.")
            end
        end,
    })

    -- [TAB ÂM NHẠC - PROMAX EDITION (UPGRADED TO LIST VIEW)]
    local TabMusic = Window:CreateTab("Âm Nhạc 🎵", 14420842855)
    TabMusic:CreateSection("📌 Trình phát nhạc")

    TabMusic:CreateSlider({
       Name = "🔊 Âm Lượng Nhạc",
       Range = {0, 10},
       Increment = 0.5,
       CurrentValue = 2,
       Callback = function(Value)
           CurrentVolume = Value
           if CurrentMusic then
               CurrentMusic.Volume = Value
           end
       end,
    })

    TabMusic:CreateButton({
        Name = "🛑 Dừng Nhạc Đang Phát",
        Callback = function()
            if CurrentMusic then
                CurrentMusic:Stop()
                CurrentMusic:Destroy()
                CurrentMusic = nil
                Notify("Music", "Đã dừng tất cả âm nhạc!")
            else
                Notify("Music", "Hiện không có nhạc nào đang phát.")
            end
        end
    })

    TabMusic:CreateSection("🔎 Tìm kiếm & Danh sách bài hát")

    -- Database bài hát
    local MusicDatabase = {
        ["Em là gió"] = "138802194906464",
        ["Morning"] = "140722099430139",
        ["Dịu dàng"] = "140695633562281",
        ["Phonk(Super slowed)"] = "127521546166507",
        ["Deadlines"] = "140628387917926",
        ["Lời hứa sóng cuốn"] = "134473054863425",
        ["Chạm Lặng"] = "106534572994709",
        ["Nothin On Me"] = "9048375035",
        ["Lofi VietNam Christmas"] = "129361584564255",
        ["Nắng Nhạt Dần"] = "128310680553165",
        ["Mộng Hoa Bay"] = "121439292363731",
        ["Tấm Gương Ký Ức"] = "114337890778326",
        ["Đêm Cuối Cùng"] = "110842976047767",
        ["Trái Tim Lạc Lối"] = "86048865916358",
        ["Dưới Ánh Đèn Đêm"] = "139254533597672",
        ["Mơ Một Lần"] = "77538934822932"
    }

    -- Tạo danh sách tên bài hát cho Dropdown ban đầu
    local FullSongList = {}
    for name, _ in pairs(MusicDatabase) do
        table.insert(FullSongList, name)
    end
    table.sort(FullSongList)

    local CurrentSelectedSongName = ""

    local PlaylistDropdown = TabMusic:CreateDropdown({
        Name = "Chọn bài hát từ danh sách (Playlist)",
        Options = FullSongList,
        CurrentOption = {"Chọn bài hát..."},
        MultipleOptions = false,
        Flag = "MusicPlaylistDropdown",
        Callback = function(Options)
            CurrentSelectedSongName = Options[1]
            -- Tự động phát khi chọn từ danh sách nếu muốn, hoặc nhấn nút bên dưới
            if CurrentSelectedSongName and MusicDatabase[CurrentSelectedSongName] then
                PlayMusic(MusicDatabase[CurrentSelectedSongName], CurrentSelectedSongName)
            end
        end,
    })

    TabMusic:CreateInput({
        Name = "Nhập tên để lọc danh sách",
        PlaceholderText = "Tìm kiếm nhanh...",
        RemoveTextAfterFocusLost = false,
        Callback = function(Text)
            local FilteredList = {}
            if Text == "" then
                PlaylistDropdown:Refresh(FullSongList, true)
                return
            end
            
            local lowerText = string.lower(Text)
            for name, _ in pairs(MusicDatabase) do
                if string.find(string.lower(name), lowerText) then
                    table.insert(FilteredList, name)
                end
            end
            
            if #FilteredList > 0 then
                table.sort(FilteredList)
                PlaylistDropdown:Refresh(FilteredList, true)
            else
                PlaylistDropdown:Refresh({"❌ Không tìm thấy!"}, true)
            end
        end,
    })

    TabMusic:CreateSection("🔍 Phát nhạc theo ID tùy chỉnh")
    local CustomID = ""
    TabMusic:CreateInput({
       Name = "Nhập Sound ID thủ công",
       PlaceholderText = "Ví dụ: 127521546166507",
       RemoveTextAfterFocusLost = false,
       Callback = function(Text)
          CustomID = Text
       end,
    })

    TabMusic:CreateButton({
        Name = "🎵 Phát theo ID đã nhập",
        Callback = function()
            if CustomID ~= "" then
                PlayMusic(CustomID, "Custom Sound ("..CustomID..")")
            else
                Notify("Lỗi", "Vui lòng nhập ID nhạc trước!")
            end
        end
    })

    -- [TAB TỐI ƯU HÓA]
    local TabOptimize = Window:CreateTab("Tối ưu hóa", 78754569296388)
    TabOptimize:CreateSection("📌 Tối ưu hóa hiệu suất")
TabOptimize:CreateButton({ Name = "🚀 Fix Lag Turbo", Callback = function() 
        Notify("Fix Lag Turbo", "Đang load script...")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/TurboLite/Script/main/FixLag.lua"))()
        Notify("Fix Lag Turbo", "Hoàn thành load script!")
    end })
    TabOptimize:CreateButton({
        Name = "📉 Low Texture (Giảm đồ họa & Tăng FPS)",
        Callback = function()
            pcall(function()
                for _, v in pairs(workspace:GetDescendants()) do
                    if v:IsA("BasePart") and not v:IsA("MeshPart") then
                        v.Material = Enum.Material.SmoothPlastic
                        v.Reflectance = 0
                    elseif v:IsA("Decal") or v:IsA("Texture") then
                        v.Transparency = 1
                    end
                end
                game.Lighting.GlobalShadows = false
                game.Lighting.FogEnd = 9e9
                settings().Rendering.QualityLevel = 1
                Notify("Tối ưu hóa", "Đã giảm chất lượng hình ảnh xuống mức thấp nhất!")
            end)
        end
    })

    TabOptimize:CreateButton({
        Name = "🧹 Xóa Chat (Clear Chat Rác)",
        Callback = function()
            pcall(function()
                local chatFrame = game:GetService("Players").LocalPlayer.PlayerGui:FindFirstChild("Chat")
                if chatFrame then
                    local scroller = chatFrame:FindFirstChild("Frame_MessageLogDisplay", true)
                    if scroller and scroller:FindFirstChild("Scroller") then
                        for _, msg in pairs(scroller.Scroller:GetChildren()) do
                            if msg:IsA("Frame") then
                                msg:Destroy()
                            end
                        end
                        Notify("Tối ưu hóa", "Đã xóa toàn bộ tin nhắn rác trong khung chat!")
                    else
                        Notify("Tối ưu hóa", "Không tìm thấy dữ liệu chat!")
                    end
                else
                    Notify("Tối ưu hóa", "Game sử dụng TextChatService mới hoặc Custom Chat!")
                end
            end)
        end
    })

    local HidePlayersConnection = nil
    TabOptimize:CreateToggle({
        Name = "👻 Ẩn người chơi khác (Giảm tải CPU)",
        CurrentValue = false,
        Callback = function(Value)
            if Value then
                HidePlayersConnection = game:GetService("RunService").RenderStepped:Connect(function()
                    pcall(function()
                        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
                            if p ~= game:GetService("Players").LocalPlayer and p.Character then
                                for _, part in pairs(p.Character:GetDescendants()) do
                                    if part:IsA("BasePart") or part:IsA("Decal") then
                                        part.Transparency = 1
                                    end
                                end
                            end
                        end
                    end)
                end)
                Notify("Tối ưu hóa", "Đã ẩn cơ thể người chơi khác (chỉ hiện tên)!")
            else
                if HidePlayersConnection then 
                    HidePlayersConnection:Disconnect() 
                    HidePlayersConnection = nil
                end
                pcall(function()
                    for _, p in pairs(game:GetService("Players"):GetPlayers()) do
                        if p ~= game:GetService("Players").LocalPlayer and p.Character then
                            for _, part in pairs(p.Character:GetDescendants()) do
                                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                                    part.Transparency = 0
                                elseif part:IsA("Decal") then
                                    part.Transparency = 0
                                end
                            end
                        end
                    end
                end)
                Notify("Tối ưu hóa", "Đã hiển thị lại người chơi khác!")
            end
        end
    })

    -- [TAB CÀI ĐẶT]
    local TabSettings = Window:CreateTab("Cài đặt", 11956055886)
    TabSettings:CreateSection("📌 Tùy chỉnh hệ thống")
   
    _G.AntiAFK = false
    TabSettings:CreateToggle({
       Name = "Bật Anti-AFK",
       CurrentValue = false,
       Flag = "AFK_Flag",
       Callback = function(v) 
           _G.AntiAFK = v 
if v then Notify("67 Hub", "Đã bật chế độ Anti-AFK") else Notify("67 Hub", "Đã tắt chế độ Anti-AFK") end
       end,
    })

    TabSettings:CreateToggle({
        Name = "Màn hình Trắng (AFK Farm)",
        CurrentValue = false,
        Callback = function(Value)
            AFKFrame.BackgroundColor3 = Color3.new(1, 1, 1)
            AFKFrame.Visible = Value
        end,
    })

    TabSettings:CreateToggle({
        Name = "Màn hình Đen (AFK Farm)",
        CurrentValue = false,
        Callback = function(Value)
            AFKFrame.BackgroundColor3 = Color3.new(0, 0, 0)
            AFKFrame.Visible = Value
        end,
    })

    TabSettings:CreateSection("📌 Quản lý Menu")
    TabSettings:CreateButton({ 
        Name = "❌ Đóng Menu Script", 
        Callback = function() 
            -- 1. TẮT NHẠC (Bọc pcall chống lỗi Obfuscate)
            pcall(function()
                if CurrentMusic then
                    CurrentMusic:Stop()
                    CurrentMusic:Destroy()
                    CurrentMusic = nil
                end
            end)

            -- 2. TẮT CÁC CHỨC NĂNG MENU ĐANG CHẠY NGẦM
            pcall(function()
                -- Tắt Anti-AFK
                _G.AntiAFK = false
                
                -- Tắt màn hình đen/trắng AFK
                if AFKFrame then AFKFrame.Visible = false end

                -- Tắt Full Bright, trả môi trường về gốc
                if Lighting and originalBrightness then
                    Lighting.Brightness = originalBrightness
                    Lighting.ClockTime = originalClockTime
                    Lighting.FogEnd = originalFogEnd
                    Lighting.GlobalShadows = originalGlobalShadows
                end

                -- Hiện lại người chơi (nếu đang bật chế độ Ẩn người chơi)
                if HidePlayersConnection then
                    HidePlayersConnection:Disconnect()
                    HidePlayersConnection = nil
                end
                
                -- Đặt lại độ trong suốt của người chơi khác về 0
                for _, p in pairs(game:GetService("Players"):GetPlayers()) do
                    if p ~= game:GetService("Players").LocalPlayer and p.Character then
                        for _, part in pairs(p.Character:GetDescendants()) do
                            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                                part.Transparency = 0
                            elseif part:IsA("Decal") then
                                part.Transparency = 0
                            end
                        end
                    end
                end
            end)

            -- 3. ĐÓNG MENU RAYFIELD
            pcall(function()
                if _G.Rayfield then
                    _G.Rayfield:Destroy()
                end
            end)
        end 
    })

    -- [TAB THÔNG TIN]
    local TabAbout = Window:CreateTab("Thông tin", 4871684504)
    TabAbout:CreateSection("📌 Thông tin chi tiết")
    TabAbout:CreateLabel("● Game Hiện Tại: " .. (GameName or "N/A"))
    TabAbout:CreateLabel("● PlaceID: " .. (PlaceID or "N/A"))
    TabAbout:CreateLabel("● Anti-Kick: Hoạt động 🟢")
    TabAbout:CreateLabel("Created by hizuma_67")

    local DiscordCopied = false
    TabAbout:CreateSection("🤝 Discord/Helps")	
    TabAbout:CreateButton({
       Name = " Cộng đồng Discord/Trợ giúp",
       Callback = function()
           if not DiscordCopied then
               setclipboard("https://discord.gg/aKAChQZRk")
               Notify("Discord", "Đã sao chép link Discord vào bộ nhớ tạm!")
               DiscordCopied = true 
           else
               Notify("Thông báo", "Bạn đã sao chép link rồi!")
           end
       end
    })
end
----------------------------------------------------------------
-- HỆ THỐNG THÔNG BÁO TOÀN CẦU (FIXED FOR OBFUSCATOR)
----------------------------------------------------------------
local function ShowGlobalAnnouncement()
    -- [!] FIX: Bọc task.spawn và pcall để Obfuscator không làm crash luồng chính
    task.spawn(function()
        local success, err = pcall(function()
            -- Lấy nội dung thông báo (Nếu bạn có link pastebin thì để ở đây, hiện tại mình giữ cấu trúc an toàn)
            -- Bọc pcall cho Rayfield để tránh lỗi khi UI chưa render xong
    if _G.Rayfield then
    _G.Rayfield:Notify({
        Title = "🌐 THÔNG BÁO TỪ ADMIN",
                    Content = "Chào mừng bạn đã sử dụng phiên bản mới nhất của 6_7 HUB!",
                    Duration = 8,
                    Image = 4483362458
                })
            else
                -- Fallback an toàn nếu Rayfield bị delay bởi Obfuscator
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "🌐 THÔNG BÁO TỪ ADMIN",
                    Text = "Chào mừng bạn đã sử dụng phiên bản mới nhất của 6_7 HUB!",
                    Duration = 8
                })
            end
        end)
    end)
end

----------------------------------------------------------------
-- CHẠY LOADING TRƯỚC, RAYFIELD SAU (CODE GỐC CỦA BẠN - ĐÃ THÊM THÔNG BÁO)
----------------------------------------------------------------
local function RunLoading()
    local steps = {
        {0.15, "🛡️ Checking Security Protocol..."},
        {0.35, "⚙️ Loading System Assets..."},
        {0.55, "💉 Injecting Main Scripts..."},
        {0.75, "🌀 Synchronizing UI Elements..."},
        {0.90, "🖥️ Finalizing Configuration..."},
        {1.0, "✨ System Ready!"}
    }
    
    for _, s in ipairs(steps) do
        -- TweenService xử lý an toàn
        pcall(function()
            TweenService:Create(BarFill, TweenInfo.new(1.0, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {Size = UDim2.new(s[1], 0, 1, 0)}):Play()
            StatusText.Text = s[2]
        end)
        task.wait(0.6) 
    end
    
    pcall(function() PlaySound("139286387133768") end)
    task.wait(0.5)
    
    -- Loading xong mới xóa UI và bật Rayfield
    if LoadingGui then 
        pcall(function() LoadingGui:Destroy() end) 
    end
    
    pcall(function() StartRayfield() end)
    
    -- [!] GỌI THÔNG BÁO TOÀN CẦU NGAY SAU KHI UI RAYFIELD KHỞI ĐỘNG XONG
    -- Gọi qua pcall để tránh mọi rủi ro do Obfuscator gây ra
    pcall(function()
        ShowGlobalAnnouncement()
    end)
end

-- Chạy hệ thống
task.spawn(function()
    pcall(function()
        RunLoading()
    end)
end)
----------------------------------------------------------------
-- HỆ THỐNG ANTI-AFK CHẠY NGẦM
----------------------------------------------------------------
Player.Idled:Connect(function()
    if _G.AntiAFK then
        pcall(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
end)